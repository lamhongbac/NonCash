@inherits LayoutComponentBase
@implements IDisposable
@inject NonCash.Web.Services.ClientAuthService AuthState

<MudThemeProvider />
<MudDialogProvider />
<MudSnackbarProvider />
<MudPopoverProvider />

<MudLayout>
    <MudAppBar Elevation="1">
        <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick="@((e) => DrawerToggle())" />
        <MudText Typo="Typo.h6" Class="ml-3">NonCash</MudText>
        <MudSpacer />
        @if (AuthState.IsLoggedIn)
        {
            <MudText Typo="Typo.body2" Class="mr-3 mt-1" Color="Color.Primary">@AuthState.FullName (@AuthState.Role)</MudText>
            <MudButton Color="Color.Inherit" Variant="Variant.Text" OnClick="HandleLogout">
                <MudIcon Icon="@Icons.Material.Filled.Logout" Class="mr-1" /> Logout
            </MudButton>
        }
        else
        {
            <MudButton Href="/member-login" Color="Color.Inherit" Variant="Variant.Text">Customer Login</MudButton>
            <MudButton Href="/login" Color="Color.Inherit" Variant="Variant.Text">Admin / Brand Staff Login</MudButton>
        }
    </MudAppBar>
    <MudDrawer @bind-Open="_drawerOpen" ClipMode="DrawerClipMode.Always" Elevation="2">
        <MudNavMenu>
            <MudNavLink Href="/" Match="NavLinkMatch.All" Icon="@Icons.Material.Filled.Dashboard">Dashboard</MudNavLink>
            <MudDivider Class="my-2" />
            @if (!AuthState.IsLoggedIn)
            {
                <MudText Typo="Typo.caption" Class="px-4">Public</MudText>
                <MudNavLink Href="/member-register" Icon="@Icons.Material.Filled.PersonAdd">Member Registration</MudNavLink>
                <MudNavLink Href="/register" Icon="@Icons.Material.Filled.AppRegistration">Business Registration</MudNavLink>
                <MudDivider Class="my-2" />
            }
            @if (AuthState.IsLoggedIn && AuthState.Role == "Admin")
            {
                <MudText Typo="Typo.caption" Class="px-4">Admin</MudText>
                <MudNavLink Href="/admin/registration-requests" Icon="@Icons.Material.Filled.AppRegistration">Registration Requests</MudNavLink>
                <MudNavLink Href="/admin/businesses" Icon="@Icons.Material.Filled.AccountBalance">Businesses</MudNavLink>
                <MudNavLink Href="/admin/brands" Icon="@Icons.Material.Filled.Business">Brands</MudNavLink>
                <MudNavLink Href="/admin/users" Icon="@Icons.Material.Filled.People">Users</MudNavLink>
                <MudNavLink Href="/admin/credits" Icon="@Icons.Material.Filled.Toll">Credit Management</MudNavLink>
                <MudNavLink Href="/admin/credit-policies" Icon="@Icons.Material.Filled.Policy">Pricing Policies</MudNavLink>
                <MudNavLink Href="/admin/welcome-policy-templates" Icon="@Icons.Material.Filled.CardMembership">Welcome Policy Templates</MudNavLink>
                <MudNavLink Href="/admin/contract-templates" Icon="@Icons.Material.Filled.Description">Contract Templates</MudNavLink>
                <MudNavLink Href="/admin/subscription-fee-policies" Icon="@Icons.Material.Filled.AttachMoney">Subscription Fee Policies</MudNavLink>
                <MudNavLink Href="/admin/welcome-policies" Icon="@Icons.Material.Filled.CardGiftcard">Welcome Policies</MudNavLink>
                <MudNavLink Href="/admin/credit-adjustments" Icon="@Icons.Material.Filled.Rule">Credit Adjustments</MudNavLink>
                <MudNavLink Href="/admin/integration-partners" Icon="@Icons.Material.Filled.Api">Integration Partners</MudNavLink>
                <MudDivider Class="my-2" />
            }
            @if (AuthState.IsLoggedIn && AuthState.Role == "FinancialController")
            {
                <MudText Typo="Typo.caption" Class="px-4">Financial Controller</MudText>
                <MudNavLink Href="/admin/credit-adjustments" Icon="@Icons.Material.Filled.Rule">Adjustment Approvals</MudNavLink>
                <MudDivider Class="my-2" />
            }
            @if (AuthState.IsLoggedIn && AuthState.Role == "BrandManager")
            {
                <MudText Typo="Typo.caption" Class="px-4">Brand Manager</MudText>
                <MudNavLink Href="/brandmanager/plans" Icon="@Icons.Material.Filled.Assignment">Voucher Plans</MudNavLink>
                <MudNavLink Href="/brandmanager/outlets" Icon="@Icons.Material.Filled.Store">Outlets</MudNavLink>
                <MudNavLink Href="/brandmanager/customers" Icon="@Icons.Material.Filled.Contacts">Customers</MudNavLink>
                <MudNavLink Href="/brandmanager/credits" Icon="@Icons.Material.Filled.Toll">My Credits</MudNavLink>
                <MudNavLink Href="/pos/redeem" Icon="@Icons.Material.Filled.PointOfSale">POS Redemption</MudNavLink>
                <MudDivider Class="my-2" />
            }
            @if (AuthState.IsLoggedIn && AuthState.IsMember)
            {
                <MudText Typo="Typo.caption" Class="px-4">Member</MudText>
                <MudNavLink Href="/store" Icon="@Icons.Material.Filled.Storefront">Voucher Store</MudNavLink>
                <MudNavLink Href="/my-vouchers" Icon="@Icons.Material.Filled.CardGiftcard">My Vouchers</MudNavLink>
                <MudNavLink Href="/transfers" Icon="@Icons.Material.Filled.SwapHoriz">Transfers</MudNavLink>
                <MudDivider Class="my-2" />
            }
        </MudNavMenu>
    </MudDrawer>
    <MudMainContent>
        <MudContainer MaxWidth="MaxWidth.Large" Class="pt-4">
            @Body
        </MudContainer>
    </MudMainContent>
</MudLayout>

<div id="blazor-error-ui" data-nosnippet>
    An unhandled error has occurred.
    <a href="." class="reload">Reload</a>
    <span class="dismiss">🗙</span>
</div>

@code {
    private bool _drawerOpen = true;

    protected override async Task OnInitializedAsync()
    {
        await AuthState.InitializeAsync();
        AuthState.OnAuthStateChanged += OnAuthStateChanged;

        if (AuthState.IsLoggedIn)
        {
            await AuthState.StartIdleTimerAsync();
        }
    }

    private void OnAuthStateChanged()
    {
        InvokeAsync(StateHasChanged);
    }

    public void Dispose()
    {
        AuthState.OnAuthStateChanged -= OnAuthStateChanged;
    }

    private void DrawerToggle()
    {
        _drawerOpen = !_drawerOpen;
    }

    private async Task HandleLogout()
    {
        await AuthState.LogoutAsync();
    }
}
